server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Husnain224/config-server
          default-label: main

  security:
    user:
      name: configuser
      password: configpass
